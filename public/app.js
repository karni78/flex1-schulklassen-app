const $=s=>document.querySelector(s);let admin=false;
async function api(url,opt){const r=await fetch(url,opt);if(r.status===401){location='/login';throw new Error('login')}const d=await r.json();if(!r.ok)throw new Error(d.error||'Fehler');return d}
const schedule=[['07:45 – 08:30','Lernzeit / HA',''],['08:30 – 09:15','Sachkunde','sach'],['09:15 – 10:00','Sport','sport'],['10:00 – 10:20','Pause','pause'],['10:20 – 11:05','Deutsch','deutsch'],['11:05 – 11:50','Religion','religion'],['11:50 – 12:10','Pause','pause'],['12:10 – 12:55','Mathe','mathe'],['12:55 – 13:40','Englisch','englisch'],['13:40 – 14:25','Kunst','kunst'],['14:25 – 15:10','Musik','musik']];
function showPage(id){document.querySelectorAll('.page').forEach(p=>p.classList.toggle('active',p.id===id));document.querySelectorAll('[data-page]').forEach(b=>b.classList.toggle('active',b.dataset.page===id));window.scrollTo({top:0,behavior:'smooth'});if(id==='chat')loadMessages();if(id==='gallery'||id==='docs')loadFiles()}
document.querySelectorAll('[data-page]').forEach(b=>b.addEventListener('click',()=>showPage(b.dataset.page)));
function esc(s){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function initials(name){return esc(name.trim().split(/\s+/).map(x=>x[0]).slice(0,2).join('').toUpperCase()||'?')}
function renderSchedule(){ $('#scheduleList').innerHTML=schedule.map(x=>`<div class="lesson ${x[2]}"><div class="time">${x[0]}</div><div class="subject">${x[1]}</div></div>`).join('') }
async function load(){const me=await api('/api/me');admin=me.admin;$('#admin').classList.toggle('hidden',!admin);if(admin){const a=document.createElement('button');a.dataset.page='admin';a.textContent='⚙️';a.className='icon-btn';a.onclick=()=>showPage('admin');document.querySelector('.top-actions').prepend(a)}const info=await api('/api/info');$('#infoTitle').textContent=info.title;$('#infoBody').textContent=info.body;if(admin){$('#editTitle').value=info.title;$('#editBody').value=info.body}renderSchedule();await loadMessages();await loadFiles()}
async function loadMessages(){const rows=await api('/api/messages');$('#chatCount').textContent=rows.length;$('#messages').innerHTML=rows.map(m=>`<article class="msg"><div class="avatar">${initials(m.name)}</div><div class="bubble"><div class="msg-meta"><strong>${esc(m.name)}</strong><time>${new Date(m.created_at+'Z').toLocaleString('de-DE',{dateStyle:'short',timeStyle:'short'})}</time></div><p>${esc(m.text)}</p><div class="reacts"><button onclick="react(${m.id},'👍')">👍 ${m.thumbs||0}</button><button onclick="react(${m.id},'❤️')">❤️ ${m.hearts||0}</button><button onclick="react(${m.id},'😂')">😂 ${m.laughs||0}</button><button onclick="react(${m.id},'👏')">👏 ${m.claps||0}</button></div></div></article>`).join('')||'<p class="muted">Noch keine Nachrichten. Sei der Erste! 👋</p>';$('#messages').scrollTop=$('#messages').scrollHeight}
async function react(id,reaction){await api('/api/messages/'+id+'/reactions',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({reaction})});loadMessages()}
async function loadFiles(){const files=await api('/api/files');$('#images').innerHTML=files.filter(f=>f.kind==='image').map(f=>`<article class="gallery-item"><img src="/uploads/${encodeURIComponent(f.filename)}" alt="${esc(f.title)}"><div class="caption">${esc(f.title)}</div>${admin?`<button onclick="delFile(${f.id})" style="position:absolute;right:10px;top:10px;border:0;border-radius:10px;padding:7px;background:#fff;color:#c55">×</button>`:''}</article>`).join('')||'<div class="panel"><p class="muted">Noch keine Bilder – bald gibt es hier Klassenmomente. 📸</p></div>';$('#docsList').innerHTML=files.filter(f=>f.kind==='document').map(f=>`<div class="doc"><div class="doc-icon">PDF</div><div class="doc-main"><b>${esc(f.title)}</b><small>${esc(f.original_name)}</small></div><a href="/uploads/${encodeURIComponent(f.filename)}" download>↓</a>${admin?`<button onclick="delFile(${f.id})" class="icon-btn">×</button>`:''}</div>`).join('')||'<div class="panel"><p class="muted">Noch keine Dokumente hinterlegt.</p></div>'}
async function delFile(id){if(confirm('Datei wirklich löschen?')){await api('/api/files/'+id,{method:'DELETE'});loadFiles()}}
$('#chatForm').onsubmit=async e=>{e.preventDefault();await api('/api/messages',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:$('#name').value,text:$('#text').value})});$('#text').value='';loadMessages()};$('#logout').onclick=async()=>{await fetch('/api/logout',{method:'POST'});location='/login'};$('#saveInfo').onclick=async()=>{await api('/api/info',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({title:$('#editTitle').value,body:$('#editBody').value})});load()};$('#uploadForm').onsubmit=async e=>{e.preventDefault();const fd=new FormData(e.target);await api('/api/files',{method:'POST',body:fd});e.target.reset();loadFiles()};load();setInterval(loadMessages,7000);
